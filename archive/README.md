# Archive

This folder holds **non-runtime** material: an older `OSMAGIC-Standalone` bundle, `josm-helper.py.backup`, and `helper-base64.txt`. The live app uses **`web/`**, **`tools/`**, and **`server.py`** at the repository root.
